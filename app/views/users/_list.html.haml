#chat-group-users
  - @group.users.where.not(id: current_user.id).each do |f|
    .chat-group-user.clearfix.js-chat-member
      %input{name: "group[user_ids][]", type: "hidden", value: f.id}
      %p.chat-group-user__name #{f[:name]}
      %a.user-search-remove.chat-group-user__btn.chat-group-user__btn--remove.js-remove-btn 削除
