.messages
  - @messages.each do |message|
    .message{data: {id: message.id}}
      .upper-message
        .upper-message__username
          = message.user.name
        .upper-message__date
          = message.created_at.to_s[0..15]
      .lower-message
        %p{class:"lower-message__content"}
          = message.text
          = image_tag message.image
